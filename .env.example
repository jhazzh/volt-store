# Copy to .env.local and fill from Supabase → Project Settings → API Keys
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_REF.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
# Server-only. Never expose to the browser.
SUPABASE_SECRET_KEY=sb_secret_xxx

# Server-only. Shared secret for POST /api/revalidate. Any long random string.
REVALIDATE_SECRET=

# Stripe test keys — dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_xxx
# From `stripe listen` (dev) or dashboard webhook endpoint (prod)
STRIPE_WEBHOOK_SECRET=whsec_xxx

# PayPal sandbox app — developer.paypal.com → Apps & Credentials
PAYPAL_CLIENT_ID=xxx
PAYPAL_CLIENT_SECRET=xxx
# Set to "production" for live payments
PAYPAL_ENV=sandbox

# Xendit test keys — dashboard.xendit.co → Settings → Developers → API keys
XENDIT_SECRET_KEY=xnd_development_xxx
# Settings → Developers → Callbacks → Callback verification token
XENDIT_CALLBACK_TOKEN=xxx
